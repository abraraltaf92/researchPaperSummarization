part-of-speech (POS) tag distributions are known to exhibit sparsity. a word is likely to take: Part-of-speech (POS) tag distributions are known to exhibit sparsity — a word is likely: Part-of-speech (POS) tag distributions are known to exhibit sparsity — a word is likely: POS tags are known to exhibit sparsity — a word is likely to take one predominant tag in an corpus.: Part-of-speech (POS) tag distributions exhibit sparsity — a word is likely to take: Part-of-speech (POS) tag distributions exhibit sparsity — a word is likely to take: Part-of-speech (POS) tag distributions are known to exhibit sparsity — a word is likely: Part-of-speech (POS) tag distributions are known to exhibit sparsity — a word is likely: Part-of-speech (POS) tag distributions are known to exhibit sparsity — a word is likely